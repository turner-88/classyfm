admin
- display logo.png on login screen and sidebar/topbar brand.
- fix select input height. (see schedule edit, program field. and also role input in user form).
- think better layout for broadcaster form, program form, and media form.
- unify Save button in media form.
- hide ads setting form in a button. just display implemented setting above the datatables (maybe using badge?). (see "/admin/ads"). when the button is clicked, it will open the ads setting form. when the button is clicked again, it will close the ads setting form. when the setting is saved, it will close the ads setting form too.

public
- generate icon from default-cover.jpg.
- remove "Today On Air" from home page. "On Air Now" is enough.
- think better UI for "/program" and "/broadcasters" (the exact page, not the detail page). maybe it need a hero-like section.
- think better UI for "/live" page too.

add close button to floating player on the top-right (current position of expand button, so move expand button before close button). when user click the button, it will close the floating player and convert it into floatig button with play/pause icon. when user click it, it will open the floating player again.

add new tiktok floating card above current floating player. when user click the card, it will open tiktok app. it must has close button. when user click the button, it will close the tiktok card and convert to floating button with tiktok logo. see https://therockinlife.com/ for reference.


-----------
- allow multiple broadcaster for programs and program_schedules.
- home page - welcome section: left column must be wider than right column

- increase image width and height in featured news in home page. it must have the same width with non featured news
- display space available info on empty ads placeholder.
- think what content should be before the footer in home page.

- redesign "All Programs" section in program page. use list instead of grid. list item has big image on the left, and info on the right

------
- floating player and tiktok floating card must have same fixed width.
- tiktok floating card must have 2 action: opening profile and opening live
- redesign news and news detail page (hot release)